<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasom2.mapper.testMapper">

    <select id="selectTest" resultType="String">
        SELECT * FROM testtable
    </select>
</mapper>